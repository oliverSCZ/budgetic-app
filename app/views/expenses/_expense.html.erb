<%= link_to category_expense_path(category_id: params[:category_id], id: expense.id) do %>
<div id="<%= dom_id expense %>", class="w-full bg-white px-5 py-3 flex flex-col mb-5 hover:bg-[#3778c2] hover:text-white">
<div class="flex justify-between text-lg">
  <p>
    <%= expense.name %>

  </p>

  <p>
    $<%= expense.amount %>
  </p>
   <p>
    
  </p>
</div>
    <p class="text-sm">
      <%= expense.created_at.strftime("%d %B %Y")%>
    </p>
</div>
<% end %>